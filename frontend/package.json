{
  "name": "krabville-web",
  "private": true,
  "version": "2.2.1",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "tsc --noEmit && vite build",
    "capture:readme": "node scripts/capture-readme.mjs",
    "lint": "oxlint --deny-warnings --report-unused-disable-directives src scripts vite.config.ts playwright.config.ts",
    "preview": "vite preview --host 127.0.0.1",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "lucide": "1.30.0",
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "oxlint": "1.78.0",
    "typescript": "7.0.2",
    "vite": "8.2.1"
  }
}
